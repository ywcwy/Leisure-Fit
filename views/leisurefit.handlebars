<main class="row mt-5 mx-5 d-flex justify-content-center">
  <div class="col-md-12 d-flex justify-content-center">
    <h1>{{leisurefit.name}}</h1>
  </div>
  <div class="col-md-8">
    <br>
    <div class="well">
      <ul class="list-unstyled">
        <li><strong>Training Day: {{leisurefit.Category.day}}</strong></li>
        <li class="mt-"><a href="?categoryId={{leisurefit.Category.id}}" style="color: #c9461e;">
            #{{leisurefit.Category.name}}
          </a></li><br>
        <li>{{leisurefit.description}}</li><br>
        <li class="d-flex justify-content-center">
          <img class="d-none d-sm-block w-75" style="object-fit:cover" src="{{leisurefit.image}}">
          <img class="d-block d-sm-none mh-100" style="width:85%; object-fit:cover" src="{{leisurefit.image}}">
        </li>
      </ul>
    </div>
  </div>
  <div class="col-md-12 d-flex justify-content-center my-5">
    <a href="javascript:history.back()" style="color: #31583f">回上一頁</a>
  </div>
</main>
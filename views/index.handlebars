<main>
  <div class="jumbotron" style="background-color: #31583f;">
    <h3 class="display-4 text-white">大人的體育課</h3>
    <p class="lead text-white">一起在都市的束縛中，大力呼吸，解放心靈，奔放四肢</p>
    <hr class="my-4 bg-white">
    <p class="lead">
      <a class="btn  btn-white btn-md text-white" href="#" role="button" style="background-color: #de6548;">Ｍore...</a>
    </p>
  </div>
  <div class=" container">
    <div class="row mb-3">
      <div class="col">
        <ul class="nav nav-pills">
          <li class="nav-item">
            <a class="nav-link" href="?categoryId=all">全部</a>
          </li>
          {{!-- {{#each categories}}
          <li class="nav-item">
            <a class="nav-link" href="?categoryId={{this.id}}">{{this.name}}</a>
          </li>
          {{/each}} --}}
        </ul>
      </div>
      <br />
    </div>
    <div class="row">
      {{#each leisurefits}}
      <div class="col-md-4">
        <div class="card mb-4 shadow-sm">
          <img class="card-img-top" src="{{this.image}}" alt="Card image cap" width="286px" height="180px">
          <div class="card-body">
            <p class="card-text">
              <a href="/leisurefits/{{this.id}}">
                {{this.name}}
              </a>
            </p>
            <span class="badge badge-secondary">{{this.categoryName}}</span>
            <p class="card-text">{{this.description}}</p>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
    <br />
    <hr />
    <footer>
      <div class="row">
        <div class="col-lg-12">
          <p>Copyright &copy 2021, Leisure Fit</p>
        </div>
      </div>
    </footer>
  </div>
</main>